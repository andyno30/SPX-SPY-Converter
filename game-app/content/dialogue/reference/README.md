# Local reference dialogue

Private reference only; ignored except this README. Quest logic references dialogue IDs. Public English reconstruction will be separately authored with line-level provenance in `content/dialogue/public/`.
