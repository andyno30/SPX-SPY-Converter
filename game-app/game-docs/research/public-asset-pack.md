# Public replacement pack

No art or music is approved yet. The empty manifest is intentional and is not a complete public game pack. Add each commissioned/original replacement with path, media type, license, creator and source record. The engine resolves logical IDs and rejects missing mappings. The build rejects all public files except the manifest and explicitly approved entries before Next.js copies the public directory.
